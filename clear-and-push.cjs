'use strict';
/**
 * clear-and-push.cjs
 * Drops every table in the public schema, then runs drizzle-kit push
 * to create the full schema from scratch.
 * Runs as part of the production build step.
 */
const { execSync } = require('child_process');
const { Pool }     = require('pg');

async function main() {
  const url = process.env.DATABASE_URL;
  if (!url) { console.log('[RESET] No DATABASE_URL — skipping.'); return; }

  const pool   = new Pool({ connectionString: url, ssl: url.includes('neon') ? { rejectUnauthorized: false } : false });
  const client = await pool.connect();

  try {
    console.log('[RESET] Dropping all tables...');
    await client.query('DROP SCHEMA public CASCADE');
    await client.query('CREATE SCHEMA public');
    await client.query('GRANT ALL ON SCHEMA public TO public');
    console.log('[RESET] Schema cleared.');

    console.log('[RESET] Pushing core schema (drizzle-kit)...');
    execSync('npx drizzle-kit push --force', { stdio: 'inherit' });
    console.log('[RESET] Core schema pushed.');

    console.log('[RESET] Creating extended tables...');
    const extended = [
      `CREATE TABLE IF NOT EXISTS "pipelines" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"stages" jsonb DEFAULT '[]'::jsonb,"is_default" boolean DEFAULT false,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "invoices" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"number" text NOT NULL,"contact_id" uuid REFERENCES contacts(id),"account_id" uuid REFERENCES accounts(id),"deal_id" uuid REFERENCES deals(id),"status" text DEFAULT 'draft',"subtotal" numeric(12,2) DEFAULT 0,"tax" numeric(12,2) DEFAULT 0,"total" numeric(12,2) DEFAULT 0,"currency" text DEFAULT 'USD',"due_date" timestamp,"paid_at" timestamp,"notes" text,"items" jsonb DEFAULT '[]'::jsonb,"created_by" uuid REFERENCES users(id),"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "ai_usage" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"user_id" uuid REFERENCES users(id),"type" text NOT NULL,"tokens" integer DEFAULT 0,"cost" numeric(8,6) DEFAULT 0,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "agents" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"type" text NOT NULL,"name" text NOT NULL,"is_active" boolean DEFAULT true,"personality" text,"total_sessions" integer DEFAULT 0,"total_messages" integer DEFAULT 0,"last_active_at" timestamp,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "agent_memories" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"agent_type" text NOT NULL,"category" text NOT NULL,"key" text NOT NULL,"value" text NOT NULL,"importance" integer DEFAULT 5,"source" text,"related_entity_id" uuid,"related_entity_type" text,"expires_at" timestamp,"usage_count" integer DEFAULT 0,"last_used_at" timestamp,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "agent_sessions" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"user_id" uuid NOT NULL REFERENCES users(id),"agent_type" text NOT NULL,"title" text,"summary" text,"message_count" integer DEFAULT 0,"tokens_used" integer DEFAULT 0,"is_active" boolean DEFAULT true,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "agent_messages" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"session_id" uuid NOT NULL REFERENCES agent_sessions(id) ON DELETE CASCADE,"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"role" text NOT NULL,"content" text NOT NULL,"tokens_used" integer DEFAULT 0,"tool_calls" jsonb DEFAULT '[]'::jsonb,"memories_used" jsonb DEFAULT '[]'::jsonb,"memories_created" jsonb DEFAULT '[]'::jsonb,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "agent_tasks" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"agent_type" text NOT NULL,"session_id" uuid REFERENCES agent_sessions(id),"title" text NOT NULL,"description" text,"status" text DEFAULT 'pending',"priority" text DEFAULT 'medium',"result" text,"assigned_to_user_id" uuid REFERENCES users(id),"due_at" timestamp,"completed_at" timestamp,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "agent_lead_gen_results" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"session_id" uuid REFERENCES agent_sessions(id),"first_name" text,"last_name" text,"email" text,"company" text,"job_title" text,"phone" text,"source" text,"score" integer DEFAULT 0,"enrichment_data" jsonb DEFAULT '{}'::jsonb,"outreach_status" text DEFAULT 'pending',"imported_as_lead_id" uuid REFERENCES leads(id),"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "companies" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"domain" text,"website" text,"industry" text,"sub_industry" text,"size" text,"employee_count" integer,"revenue" text,"founded" integer,"hq_city" text,"hq_state" text,"hq_country" text,"description" text,"linkedin_url" text,"twitter_url" text,"crunchbase_url" text,"tech_stack" jsonb DEFAULT '[]'::jsonb,"keywords" jsonb DEFAULT '[]'::jsonb,"funding_stage" text,"total_funding" text,"last_funding_date" text,"intent_signals" jsonb DEFAULT '[]'::jsonb,"score" integer DEFAULT 0,"tags" jsonb DEFAULT '[]'::jsonb,"data_source" text DEFAULT 'enriched',"last_enriched_at" timestamp,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "prospects" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"company_id" uuid REFERENCES companies(id),"first_name" text NOT NULL,"last_name" text,"email" text,"email_status" text DEFAULT 'unknown',"work_email" text,"personal_email" text,"phone" text,"direct_phone" text,"mobile_phone" text,"job_title" text,"seniority" text,"department" text,"company" text,"company_domain" text,"linkedin_url" text,"twitter_url" text,"location" text,"city" text,"country" text,"timezone" text,"bio" text,"score" integer DEFAULT 0,"intent_score" integer DEFAULT 0,"buying_signals" jsonb DEFAULT '[]'::jsonb,"tech_stack" jsonb DEFAULT '[]'::jsonb,"skills" jsonb DEFAULT '[]'::jsonb,"tags" jsonb DEFAULT '[]'::jsonb,"outreach_status" text DEFAULT 'new',"last_contacted_at" timestamp,"sequence_id" uuid,"imported_as_lead_id" uuid REFERENCES leads(id),"imported_as_contact_id" uuid REFERENCES contacts(id),"data_source" text DEFAULT 'enriched',"last_enriched_at" timestamp,"verified_at" timestamp,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "prospect_lists" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"description" text,"filters" jsonb DEFAULT '{}'::jsonb,"prospect_count" integer DEFAULT 0,"created_by" uuid REFERENCES users(id),"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "sequences" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"description" text,"status" text DEFAULT 'draft',"steps" jsonb DEFAULT '[]'::jsonb,"total_enrolled" integer DEFAULT 0,"total_replied" integer DEFAULT 0,"total_converted" integer DEFAULT 0,"created_by" uuid REFERENCES users(id),"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "website_visitors" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"visitor_id" text,"company_name" text,"company_domain" text,"ip_address" text,"country" text,"city" text,"pages" jsonb DEFAULT '[]'::jsonb,"session_count" integer DEFAULT 1,"total_time_on_site" integer DEFAULT 0,"last_seen" timestamp DEFAULT now(),"first_seen" timestamp DEFAULT now(),"score" integer DEFAULT 0,"identified_as_prospect_id" uuid REFERENCES prospects(id),"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "technographics" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"company_id" uuid REFERENCES companies(id),"company_domain" text,"technology" text NOT NULL,"category" text,"vendor" text,"confidence" integer DEFAULT 80,"detected_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "intent_signals" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"company_domain" text,"company_name" text,"topic" text NOT NULL,"signal_type" text NOT NULL,"strength" integer DEFAULT 50,"description" text,"source_url" text,"detected_at" timestamp DEFAULT now(),"expires_at" timestamp)`,
      `CREATE TABLE IF NOT EXISTS "health_checks" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"check_type" text NOT NULL,"status" text NOT NULL,"latency_ms" integer,"message" text,"details" jsonb DEFAULT '{}'::jsonb,"checked_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "error_logs" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid,"severity" text NOT NULL,"category" text NOT NULL,"message" text NOT NULL,"stack" text,"context" jsonb DEFAULT '{}'::jsonb,"user_id" uuid,"endpoint" text,"resolved" boolean DEFAULT false,"resolved_at" timestamp,"resolved_by" text,"healing_attempts" integer DEFAULT 0,"healing_log" jsonb DEFAULT '[]'::jsonb,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "healing_rules" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"name" text NOT NULL,"description" text,"trigger" text NOT NULL,"pattern" text NOT NULL,"category" text NOT NULL,"action" text NOT NULL,"action_config" jsonb DEFAULT '{}'::jsonb,"is_active" boolean DEFAULT true,"success_count" integer DEFAULT 0,"failure_count" integer DEFAULT 0,"last_triggered_at" timestamp,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "performance_metrics" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"metric_type" text NOT NULL,"value" numeric(10,4) NOT NULL,"unit" text,"endpoint" text,"tags" jsonb DEFAULT '{}'::jsonb,"recorded_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "seo_projects" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"domain" text NOT NULL,"country" text DEFAULT 'US',"language" text DEFAULT 'en',"competitors" jsonb DEFAULT '[]'::jsonb,"is_active" boolean DEFAULT true,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "seo_audits" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"project_id" uuid REFERENCES seo_projects(id),"score" integer DEFAULT 0,"issues" jsonb DEFAULT '[]'::jsonb,"summary" jsonb DEFAULT '{}'::jsonb,"crawled_at" timestamp DEFAULT now(),"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "content_ideas" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"project_id" uuid REFERENCES seo_projects(id),"title" text NOT NULL,"keyword" text,"search_volume" integer DEFAULT 0,"difficulty" integer DEFAULT 0,"content_type" text DEFAULT 'blog',"outline" jsonb DEFAULT '[]'::jsonb,"status" text DEFAULT 'idea',"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "products" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"store_id" uuid REFERENCES stores(id),"name" text NOT NULL,"slug" text,"description" text,"short_description" text,"sku" text,"barcode" text,"price" numeric(12,2) NOT NULL DEFAULT 0,"compare_at_price" numeric(12,2),"cost" numeric(12,2),"currency" text DEFAULT 'USD',"category" text,"tags" jsonb DEFAULT '[]'::jsonb,"images" jsonb DEFAULT '[]'::jsonb,"weight" numeric(8,2),"dimensions" jsonb DEFAULT '{}'::jsonb,"inventory" integer DEFAULT 0,"low_stock_threshold" integer DEFAULT 10,"track_inventory" boolean DEFAULT true,"is_available" boolean DEFAULT true,"is_featured" boolean DEFAULT false,"ai_score" integer DEFAULT 0,"attributes" jsonb DEFAULT '{}'::jsonb,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "orders" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"store_id" uuid REFERENCES stores(id),"order_number" text NOT NULL,"status" text DEFAULT 'pending',"customer_name" text,"customer_email" text,"customer_phone" text,"shipping_address" jsonb DEFAULT '{}'::jsonb,"items" jsonb DEFAULT '[]'::jsonb,"subtotal" numeric(12,2) DEFAULT 0,"tax" numeric(12,2) DEFAULT 0,"shipping" numeric(12,2) DEFAULT 0,"total" numeric(12,2) DEFAULT 0,"currency" text DEFAULT 'USD',"payment_status" text DEFAULT 'unpaid',"payment_method" text,"notes" text,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "transactions" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"type" text NOT NULL,"category" text,"description" text NOT NULL,"amount" numeric(15,2) NOT NULL,"currency" text DEFAULT 'USD',"exchange_rate" numeric(10,6) DEFAULT 1,"amount_usd" numeric(15,2),"date" timestamp NOT NULL,"account_id" text,"contact_id" uuid REFERENCES contacts(id),"invoice_id" uuid REFERENCES invoices(id),"tags" jsonb DEFAULT '[]'::jsonb,"reconciled" boolean DEFAULT false,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "bank_accounts" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"type" text DEFAULT 'checking',"currency" text DEFAULT 'USD',"balance" numeric(15,2) DEFAULT 0,"institution" text,"account_number" text,"is_active" boolean DEFAULT true,"last_sync_at" timestamp,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "tax_rates" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"rate" numeric(6,4) NOT NULL,"country" text,"region" text,"is_default" boolean DEFAULT false,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "employees" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"user_id" uuid REFERENCES users(id),"first_name" text NOT NULL,"last_name" text,"email" text,"phone" text,"avatar" text,"department" text,"job_title" text,"employment_type" text DEFAULT 'full_time',"status" text DEFAULT 'active',"start_date" timestamp,"end_date" timestamp,"salary" numeric(12,2),"currency" text DEFAULT 'USD',"manager_id" uuid,"skills" jsonb DEFAULT '[]'::jsonb,"location" text,"timezone" text,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "project_tasks" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"project_id" uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE,"title" text NOT NULL,"description" text,"status" text DEFAULT 'todo',"priority" text DEFAULT 'medium',"assignee_id" uuid REFERENCES users(id),"start_date" timestamp,"due_date" timestamp,"completed_at" timestamp,"estimated_hours" numeric(6,2),"actual_hours" numeric(6,2),"parent_id" uuid,"order" integer DEFAULT 0,"dependencies" jsonb DEFAULT '[]'::jsonb,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "documents" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"type" text DEFAULT 'file',"mime_type" text,"size" integer DEFAULT 0,"url" text,"content" text,"parent_id" uuid,"tags" jsonb DEFAULT '[]'::jsonb,"shared_with" jsonb DEFAULT '[]'::jsonb,"uploaded_by" uuid REFERENCES users(id),"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "funnels" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"name" text NOT NULL,"description" text,"offer" text,"target_audience" text,"status" text DEFAULT 'draft',"steps" jsonb DEFAULT '[]'::jsonb,"email_sequence" jsonb DEFAULT '[]'::jsonb,"ad_copy" jsonb DEFAULT '[]'::jsonb,"landing_page_id" uuid REFERENCES landing_pages(id),"total_leads" integer DEFAULT 0,"total_conversions" integer DEFAULT 0,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "reputation_reviews" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"platform" text NOT NULL,"reviewer_name" text,"rating" integer DEFAULT 5,"content" text,"response" text,"sentiment" text DEFAULT 'positive',"url" text,"published_at" timestamp,"responded_at" timestamp,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "client_portal_access" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"contact_id" uuid REFERENCES contacts(id),"email" text NOT NULL,"access_token" text,"permissions" jsonb DEFAULT '["view_projects","view_invoices"]'::jsonb,"last_login_at" timestamp,"is_active" boolean DEFAULT true,"created_at" timestamp DEFAULT now())`,
      `CREATE TABLE IF NOT EXISTS "whitelabel_settings" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"tenant_id" uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE UNIQUE,"brand_name" text,"logo" text,"favicon" text,"primary_color" text DEFAULT '#3b82f6',"secondary_color" text DEFAULT '#8b5cf6',"custom_domain" text,"custom_css" text,"email_from_name" text,"email_from_address" text,"support_email" text,"privacy_url" text,"terms_url" text,"hide_argilette_branding" boolean DEFAULT false,"created_at" timestamp DEFAULT now(),"updated_at" timestamp DEFAULT now())`,
    ];

    for (const stmt of extended) {
      try { await client.query(stmt); }
      catch (e) { console.warn('[RESET] Warning creating extended table:', e.message); }
    }
    console.log('[RESET] Extended tables created. Database fully ready.');
  } finally {
    client.release();
    await pool.end();
  }
}

main().catch(e => { console.error('[RESET] Fatal:', e.message); process.exit(1); });
